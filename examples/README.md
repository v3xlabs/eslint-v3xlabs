# Examples

Standalone, illustrative flat-config examples showing how `eslint-plugin-v3xlabs` can be consumed in a project's `eslint.config.mjs`.

These files are referenced by the project for reference only. They are not part of the published package and are not loaded when linting this repository.
